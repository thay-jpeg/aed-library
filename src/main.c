//Arquivo principal

#include <stdio.h>
#include "bibliotec.h"

int main() {
    int escolha;
    //print_menu(); //Função para imprimir o menu
    printf("Digite o numero do menu correspondente a opcao que deseja:\n");
    scanf("%d", &escolha);
    //processar_sistema(escolha); //Chama a função switch-case

    return 0;
}